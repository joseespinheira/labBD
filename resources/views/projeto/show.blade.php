@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">Projeto {{ $projeto->id_projeto }}</div>
                    <div class="card-body">

                        <a href="{{ url('/projeto') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/projeto/' . $projeto->id_projeto . '/edit') }}" title="Edit Projeto"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('projeto' . '/' . $projeto->id_projeto) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete Projeto" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $projeto->id_projeto }}</td>
                                    </tr>
                                    <tr><th> Id Projeto </th><td> {{ $projeto->id_projeto }} </td></tr><tr><th> Titulo </th><td> {{ $projeto->titulo }} </td></tr><tr><th> Conceito </th><td> {{ $projeto->conceito }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
