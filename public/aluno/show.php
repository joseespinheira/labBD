@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">Pessoa {{ $pessoa->matricula_pessoa }}</div>
                    <div class="card-body">

                        <a href="{{ url('/pessoa') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/pessoa/' . $pessoa->matricula_pessoa . '/edit') }}" title="Edit Pessoa"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('pessoa' . '/' . $pessoa->matricula_pessoa) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete Pessoa" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $pessoa->matricula_pessoa }}</td>
                                    </tr>
                                    <tr><th> Matricula Pessoa </th><td> {{ $pessoa->matricula_pessoa }} </td></tr><tr><th> Nome </th><td> {{ $pessoa->nome }} </td></tr><tr><th> Sexo </th><td> {{ $pessoa->sexo }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
